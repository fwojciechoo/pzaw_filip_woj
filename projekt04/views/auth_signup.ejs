<%- include("head.partial.ejs") %>

<main>
  <h1><%= title %></h1>
  <div>
    <%- include("forms/generic.ejs", form) %>
  </div>
  <span>Masz już konto? <a href="/auth/login<%- (locals.nextUrl ? `?next=${encodeURIComponent(nextUrl)}` : '') %>">Zaloguj się!</a></span>
</main>

<%- include("foot.partial.ejs") %>